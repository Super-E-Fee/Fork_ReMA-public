hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath: []
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.mode=RUN
    task:
    - trainer.project_name=my_study
    - trainer.experiment_name=rema_replica
    - trainer.nnodes=1
    - trainer.n_gpus_per_node=8
    - data.train_files=data/MATH/train_lv3to5_8k.parquet
    - data.val_files=data/overall_math/test.parquet
    - data.val_batch_size=512
    - data.train_batch_size=128
    - data.max_prompt_length=4096
    - data.max_response_length=512
    - actor_rollout_ref.model.path=/home/Documents/Fork_ReMA-public/models/Qwen2-7B-Instruct
    - actor_rollout_ref.model.use_remove_padding=True
    - actor_rollout_ref.actor.use_dynamic_bsz=True
    - actor_rollout_ref.actor.use_kl_loss=False
    - actor_rollout_ref.actor.kl_loss_coef=1e-3
    - actor_rollout_ref.actor.entropy_coeff=0
    - actor_rollout_ref.actor.ulysses_sequence_parallel_size=1
    - actor_rollout_ref.actor.ppo_mini_batch_size=512
    - actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=8
    - actor_rollout_ref.actor.clip_mode=turn
    - actor_rollout_ref.actor.agg_mode=trajectory
    - actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=32
    - actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=32
    - actor_rollout_ref.rollout.tensor_model_parallel_size=1
    - actor_rollout_ref.rollout.max_num_batched_tokens=8192
    - actor_rollout_ref.rollout.max_num_turns=30
    - actor_rollout_ref.rollout.n=16
    - actor_rollout_ref.rollout.stop_when_truncated=True
    - actor_rollout_ref.actor.optim.lr=1e-6
    - +trainer.val_before_train=True
    - +trainer.val_only=False
    - +trainer.save_val_generations=True
    - +trainer.save_train_generations=True
    - trainer.test_freq=10
    - trainer.save_freq=50
    - trainer.remove_previous_ckpt_in_save=True
    - trainer.total_epochs=10
    - trainer.total_training_steps=500
    - algorithm.adv_estimator=grpo
    - reward_model.reward_manager=rema
    - reward_model.mask_unfinished_reward=True
    - algorithm.filter_groups.enable=False
    - trainer.logger=[console,wandb]
  job:
    name: main_ppo
    chdir: null
    override_dirname: +trainer.save_train_generations=True,+trainer.save_val_generations=True,+trainer.val_before_train=True,+trainer.val_only=False,actor_rollout_ref.actor.agg_mode=trajectory,actor_rollout_ref.actor.clip_mode=turn,actor_rollout_ref.actor.entropy_coeff=0,actor_rollout_ref.actor.kl_loss_coef=1e-3,actor_rollout_ref.actor.optim.lr=1e-6,actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=8,actor_rollout_ref.actor.ppo_mini_batch_size=512,actor_rollout_ref.actor.ulysses_sequence_parallel_size=1,actor_rollout_ref.actor.use_dynamic_bsz=True,actor_rollout_ref.actor.use_kl_loss=False,actor_rollout_ref.model.path=/home/Documents/Fork_ReMA-public/models/Qwen2-7B-Instruct,actor_rollout_ref.model.use_remove_padding=True,actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=32,actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=32,actor_rollout_ref.rollout.max_num_batched_tokens=8192,actor_rollout_ref.rollout.max_num_turns=30,actor_rollout_ref.rollout.n=16,actor_rollout_ref.rollout.stop_when_truncated=True,actor_rollout_ref.rollout.tensor_model_parallel_size=1,algorithm.adv_estimator=grpo,algorithm.filter_groups.enable=False,data.max_prompt_length=4096,data.max_response_length=512,data.train_batch_size=128,data.train_files=data/MATH/train_lv3to5_8k.parquet,data.val_batch_size=512,data.val_files=data/overall_math/test.parquet,reward_model.mask_unfinished_reward=True,reward_model.reward_manager=rema,trainer.experiment_name=rema_replica,trainer.logger=[console,wandb],trainer.n_gpus_per_node=8,trainer.nnodes=1,trainer.project_name=my_study,trainer.remove_previous_ckpt_in_save=True,trainer.save_freq=50,trainer.test_freq=10,trainer.total_epochs=10,trainer.total_training_steps=500
    id: ???
    num: ???
    config_name: ppo_trainer
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.2
    version_base: '1.3'
    cwd: /home/Documents/Fork_ReMA-public
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: /home/Documents/Fork_ReMA-public/src/verl/verl/rema_trainer/config
      schema: file
      provider: main
    - path: ''
      schema: structured
      provider: schema
    output_dir: /home/Documents/Fork_ReMA-public/outputs/2025-07-11/06-11-36
    choices:
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: default
      hydra/hydra_logging: default
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
